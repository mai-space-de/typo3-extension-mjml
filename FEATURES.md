## MJML Rendering

* MJML rendering — converts `.mjml` templates to HTML via the MJML CLI binary
* Fluid integration — render MJML templates as Fluid views before converting
* Binary path configuration — configurable path to the MJML binary via extension settings
